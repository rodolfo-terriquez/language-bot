{
  "name": "japanese-language-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "Private Japanese conversation practice Telegram bot with memory, voice input, and grammar helpers",
  "main": "worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    "dev:cf": "wrangler dev",
    "deploy:cf": "wrangler deploy"
  },
  "dependencies": {
    "@upstash/qstash": "^2.7.0",
    "@upstash/redis": "^1.34.0",
    "braintrust": "^1.1.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.100.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
